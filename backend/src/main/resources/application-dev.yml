spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medicore_hms?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    org.hibernate.SQL: INFO

app:
  security:
    jwt:
      secret: ${APP_JWT_SECRET:QXBwRGV2U2VjcmV0S2V5Rm9yTWVkaUNvcmVITVMtMjAyNi0wMy0wOQ==}
